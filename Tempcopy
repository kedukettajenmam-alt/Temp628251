omniscriptHeader.js → nextStep()
[...document.querySelectorAll('*')]
  .filter(el =>
    el.checkValidity &&
    el.checkValidity() === false
  )
  .forEach(el => {
    console.log('❌ Invalid Element:', el);
    console.log('Name:', el.name || el.getAttribute('data-omni-key'));
    console.log('Value:', el.value);
  });


